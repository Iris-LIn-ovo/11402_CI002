#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main(){
	int c,s;
	while(cin>>c>>s){
		vector(int) v;
		int sum=0;
		for(int i=0; i<s; i++){
			int x;
			cin>>x;
			sum+=x;
			v.push_back(x);
		}
		int aver=sum/s;
		sort(v.begin(), v.end());
		
		if(c%2==1){
			v[n-1]
		}
		else{
			
		}
	}
	
	
	
	
	return 0;
}